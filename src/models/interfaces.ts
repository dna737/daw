export interface User {
  name: string;
  email: string;
}

export interface DogSearchOption {
  name: string;
  isSelected: boolean;
}

export interface StorageItem {
  value: any;
  expiry: number;
}
